scrape_configs:
  - job_name: multi-tenant-saas-core
    metrics_path: /actuator/prometheus
    static_configs:
      # Docker Compose: app is the service name
      - targets: ['app:8080']
        labels:
          application: multi-tenant-saas-core
